# Changelog

本文件记录每个版本的改动（最新在上）。每次发布 Release 前必须更新。

## v0.1.0 - 2026-08-17

### 新增
- dsh-session-archive：会话归档管理插件（查看已归档/全部会话）
- 一键「取消归档」：已归档会话卡片右下角按钮，恢复会话到侧栏（不删除日志）
- 两步确认永久删除死会话（运行中/当前会话锁定）
- 仓库骨架：README + LICENSE(MIT) + .gitignore + packages/ 布局
